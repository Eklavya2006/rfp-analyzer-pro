import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "RFP Analyzer Pro — Delivery Insights Platform",
  description:
    "Upload RFP documents and instantly generate cost estimates, project plans, staffing recommendations, testing strategies, and AI impact analysis.",
  keywords: ["RFP", "proposal analysis", "cost estimation", "project planning", "AI comparison"],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} h-full`}>
      <body className="h-full antialiased">{children}</body>
    </html>
  );
}
